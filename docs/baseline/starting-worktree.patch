diff --git a/app/globals.css b/app/globals.css
index 394e0ce..34e11a1 100644
--- a/app/globals.css
+++ b/app/globals.css
@@ -1,30 +1,32 @@
 @import 'tailwindcss';
 
 /* ------------------------------------------------------------------ *
- * Inferno Emails design tokens
+ * Inferno Emails design tokens - v2
  *
- * Palette comes straight off the wordmark: charcoal body, flame-red
- * accent. One accent, used identically on every section. Red is a
- * scalpel (a rule, a hover, one word in a headline), never a wash.
+ * Dark ground, one warm accent that runs flame red into ember orange.
+ * The whole system leans on three things: glass surfaces that lift on
+ * hover, a soft gradient glow used sparingly behind hero-weight moments,
+ * and one easing curve so every motion in the site feels like it came
+ * from the same hand.
  *
  * SHAPE LOCK. Exactly three radii, no exceptions:
- *   interactive (buttons, pills) .... 9999px
- *   containers  (cards, panels) ..... 0.75rem  (rounded-xl)
- *   inputs      (fields, small) ..... 0.5rem   (rounded-lg)
+ *   interactive (buttons, pills, badges) .... 9999px
+ *   containers  (cards, panels, frames) ..... 0.75rem  (rounded-xl)
+ *   inputs      (fields, small tiles) ....... 0.5rem   (rounded-lg)
  *
  * THEME LOCK. The page is dark, top to bottom. No section inverts to a
  * light background mid-scroll.
  * ------------------------------------------------------------------ */
 @theme {
-  --color-ink: #0b0b0c;
-  --color-ink-raised: #121214;
-  --color-ink-soft: #191a1d;
+  --color-ink: #09090b;
+  --color-ink-raised: #131316;
+  --color-ink-soft: #1c1c20;
 
   --color-flame: #f51717;
-  --color-ember: #e23125;
+  --color-ember: #ff7a3d;
 
-  --color-bone: #ecebe8;
-  --color-mute: #8d8a85;
+  --color-bone: #f4f3f0;
+  --color-mute: #96918a;
 
   --font-display: var(--font-archivo), 'Archivo', system-ui, sans-serif;
   --font-sans: var(--font-geist), 'Geist', system-ui, sans-serif;
@@ -34,7 +36,9 @@
   :root {
     color-scheme: dark;
     --rule: rgba(255, 255, 255, 0.09);
-    --rule-strong: rgba(255, 255, 255, 0.16);
+    --rule-strong: rgba(255, 255, 255, 0.18);
+    --ease: cubic-bezier(0.16, 1, 0.3, 1);
+    --ease-spring: cubic-bezier(0.22, 1.32, 0.4, 1);
   }
 
   * {
@@ -104,56 +108,84 @@
     .shell { padding-inline: 2.5rem; }
   }
 
-  /* Rationed to at most one per three sections. No leading dot, no
-     section numbering: the headline carries the section on its own. */
+  /* ---- glow orbs -----------------------------------------------------
+   * Soft ambient light behind hero-weight sections. Pure CSS, no assets,
+   * blurred enough to read as atmosphere rather than a shape. */
+  .orb {
+    position: absolute;
+    border-radius: 9999px;
+    filter: blur(90px);
+    pointer-events: none;
+  }
+  .orb-flame { background: radial-gradient(circle, color-mix(in srgb, var(--color-flame) 55%, transparent) 0%, transparent 72%); }
+  .orb-ember { background: radial-gradient(circle, color-mix(in srgb, var(--color-ember) 45%, transparent) 0%, transparent 72%); }
+
+  /* Small pill badge / eyebrow. Rationed to a handful across the page:
+     a live-status dot plus a short label, never a section number. */
   .eyebrow {
-    display: inline-block;
-    font-size: 0.6875rem;
+    display: inline-flex;
+    align-items: center;
+    gap: 0.55rem;
+    font-size: 0.75rem;
     font-weight: 600;
-    letter-spacing: 0.22em;
+    letter-spacing: 0.09em;
     text-transform: uppercase;
-    color: var(--color-mute);
+    color: var(--color-bone);
+    background: rgba(255, 255, 255, 0.04);
+    border: 1px solid var(--rule-strong);
+    padding: 0.45rem 1rem 0.45rem 0.75rem;
+    border-radius: 9999px;
+  }
+  .eyebrow::before {
+    content: '';
+    width: 6px;
+    height: 6px;
+    border-radius: 9999px;
+    background: var(--color-flame);
+    box-shadow: 0 0 0 3px color-mix(in srgb, var(--color-flame) 22%, transparent),
+      0 0 10px color-mix(in srgb, var(--color-flame) 80%, transparent);
+    flex: none;
   }
 
   /* ---- type scale --------------------------------------------------- *
-   * Sized so a headline of 7-9 words lands on two lines at desktop.
-   * A four-line hero headline is a font-size error, not a copy problem. */
+   * Sized so a headline of 7-9 words lands on two lines at desktop. */
   .display-xl {
     font-family: var(--font-display);
     font-weight: 800;
-    font-size: clamp(2.35rem, 4.6vw, 3.6rem);
-    line-height: 1.04;
-    letter-spacing: -0.04em;
+    font-size: clamp(2.5rem, 5.1vw, 4.1rem);
+    line-height: 1.02;
+    letter-spacing: -0.045em;
   }
   .display-lg {
     font-family: var(--font-display);
     font-weight: 700;
-    font-size: clamp(2rem, 4vw, 3.25rem);
-    line-height: 1.04;
+    font-size: clamp(2.1rem, 4.2vw, 3.4rem);
+    line-height: 1.05;
     letter-spacing: -0.035em;
   }
   .display-md {
     font-family: var(--font-display);
     font-weight: 700;
     font-size: clamp(1.5rem, 2.6vw, 2.1rem);
-    line-height: 1.08;
+    line-height: 1.1;
     letter-spacing: -0.03em;
   }
 
-  /* Emphasis stays inside the display family: italic of the same face,
-     picked out in the accent colour. Dropping a serif word into a
-     grotesk headline is a different-font-for-decoration tell. */
+  /* Emphasis stays inside the display family: same weight, a warm
+     flame-to-ember gradient clipped to the text instead of a colour
+     swap or a second typeface. */
   .accent {
-    font-style: italic;
-    color: var(--color-flame);
-    /* Italic descenders (y, g, p) clip at tight leading, so the wrapping
-       line gets extra room rather than the glyph getting cut. */
+    background: linear-gradient(100deg, var(--color-flame) 8%, var(--color-ember) 88%);
+    -webkit-background-clip: text;
+    background-clip: text;
+    color: transparent;
     line-height: 1.1;
     padding-bottom: 0.06em;
+    display: inline-block;
   }
 
   .lede {
-    font-size: clamp(1rem, 1.2vw, 1.125rem);
+    font-size: clamp(1rem, 1.2vw, 1.1875rem);
     line-height: 1.65;
     color: var(--color-mute);
     max-width: 58ch;
@@ -171,29 +203,96 @@
     padding: 0.9rem 1.5rem;
     border-radius: 9999px;
     white-space: nowrap;
-    transition: transform 0.16s ease, background-color 0.18s ease,
-      color 0.18s ease, border-color 0.18s ease;
+    transition: transform 0.35s var(--ease), box-shadow 0.35s var(--ease),
+      background-color 0.25s ease, border-color 0.25s ease, filter 0.25s ease;
   }
-  .btn:active { transform: translateY(1px); }
+  .btn:active { transform: translateY(1px) scale(0.99); }
 
-  .btn-flame { background: var(--color-flame); color: #fff; }
-  .btn-flame:hover { background: #ff2d2d; }
+  .btn-flame {
+    background: linear-gradient(135deg, var(--color-flame), var(--color-ember));
+    color: #fff;
+    box-shadow: 0 1px 1px rgba(0, 0, 0, 0.2), 0 8px 24px -10px color-mix(in srgb, var(--color-flame) 70%, transparent);
+  }
+  .btn-flame:hover {
+    transform: translateY(-2px);
+    filter: brightness(1.06);
+    box-shadow: 0 16px 34px -10px color-mix(in srgb, var(--color-flame) 75%, transparent);
+  }
 
   .btn-ghost {
     border: 1px solid var(--rule-strong);
+    background: rgba(255, 255, 255, 0.02);
     color: var(--color-bone);
   }
-  .btn-ghost:hover { border-color: var(--color-flame); color: #fff; }
+  .btn-ghost:hover {
+    border-color: color-mix(in srgb, var(--color-flame) 55%, var(--rule-strong));
+    background: rgba(255, 255, 255, 0.05);
+    transform: translateY(-2px);
+  }
 
-  .btn .arr { transition: transform 0.22s cubic-bezier(0.2, 0.7, 0.3, 1); }
-  .btn:hover .arr { transform: translateX(3px); }
+  .btn .arr { transition: transform 0.3s var(--ease-spring); }
+  .btn:hover .arr { transform: translateX(4px); }
+
+  /* ---- glass card -----------------------------------------------------
+   * The one surface style used for every card on the site: services,
+   * work, testimonials, audit checks, fit/no-fit, contact panel. Radius
+   * comes from a Tailwind class on the element (rounded-xl or rounded-lg)
+   * so the shape lock stays enforced in one place. */
+  .card {
+    background: linear-gradient(165deg, rgba(255, 255, 255, 0.045), rgba(255, 255, 255, 0.012));
+    border: 1px solid var(--rule);
+    transition: transform 0.45s var(--ease), border-color 0.4s ease,
+      box-shadow 0.45s var(--ease), background 0.4s ease;
+  }
+  .card:hover {
+    transform: translateY(-5px);
+    border-color: var(--rule-strong);
+    box-shadow: 0 24px 60px -24px rgba(0, 0, 0, 0.65);
+  }
+  .card-glow:hover {
+    border-color: color-mix(in srgb, var(--color-flame) 38%, var(--rule-strong));
+    box-shadow: 0 24px 60px -24px color-mix(in srgb, var(--color-flame) 30%, transparent);
+  }
+
+  /* ---- floating chip --------------------------------------------------
+   * Small glass badge that breaks out of a frame edge - the hero proof
+   * chip is the only place this is used. */
+  .chip {
+    display: inline-flex;
+    align-items: center;
+    gap: 0.6rem;
+    background: color-mix(in srgb, var(--color-ink) 82%, transparent);
+    border: 1px solid var(--rule-strong);
+    backdrop-filter: blur(16px);
+    border-radius: 9999px;
+    padding: 0.7rem 1.1rem;
+    box-shadow: 0 20px 44px -16px rgba(0, 0, 0, 0.7);
+  }
+
+  /* ---- form fields ------------------------------------------------- */
+  .field {
+    width: 100%;
+    border-radius: 0.5rem;
+    border: 1px solid var(--rule-strong);
+    background: rgba(255, 255, 255, 0.03);
+    color: var(--color-bone);
+    padding: 0.9rem 1rem;
+    font-size: 0.9375rem;
+    outline: none;
+    transition: border-color 0.25s ease, box-shadow 0.25s ease, background-color 0.25s ease;
+  }
+  .field::placeholder { color: color-mix(in srgb, var(--color-mute) 85%, transparent); }
+  .field:focus {
+    border-color: var(--color-flame);
+    background: rgba(255, 255, 255, 0.045);
+    box-shadow: 0 0 0 4px color-mix(in srgb, var(--color-flame) 16%, transparent);
+  }
 
   /* ---- scroll reveal ------------------------------------------------- */
   .reveal {
     opacity: 0;
-    transform: translateY(14px);
-    transition: opacity 0.6s cubic-bezier(0.16, 1, 0.3, 1),
-      transform 0.6s cubic-bezier(0.16, 1, 0.3, 1);
+    transform: translateY(20px) scale(0.985);
+    transition: opacity 0.75s var(--ease), transform 0.75s var(--ease);
   }
   .reveal.in { opacity: 1; transform: none; }
 
@@ -201,7 +300,7 @@
   .marquee {
     display: flex;
     width: max-content;
-    animation: marquee 42s linear infinite;
+    animation: marquee 38s linear infinite;
   }
   .marquee-wrap:hover .marquee { animation-play-state: paused; }
   @keyframes marquee {
@@ -212,18 +311,20 @@
   /* ---- portfolio card ------------------------------------------------ *
    * The assets are full-length email screenshots (600 x ~2200). Rather
    * than crop them to a thumbnail the card shows the top of the email and
-   * pans down the whole creative on hover: the artefact is the interaction.
-   * Justification for the motion is legibility, not decoration.          */
+   * pans down the whole creative on hover: the artefact is the interaction. */
   .email-card {
     position: relative;
     overflow: hidden;
-    border-radius: 0.75rem;
-    background: var(--color-ink-soft);
+    background: linear-gradient(165deg, rgba(255, 255, 255, 0.045), rgba(255, 255, 255, 0.012));
     border: 1px solid var(--rule);
-    transition: border-color 0.25s ease;
+    transition: transform 0.45s var(--ease), border-color 0.4s ease, box-shadow 0.45s var(--ease);
   }
   .email-card:hover,
-  .email-card:focus-within { border-color: var(--rule-strong); }
+  .email-card:focus-within {
+    transform: translateY(-5px);
+    border-color: var(--rule-strong);
+    box-shadow: 0 24px 60px -24px rgba(0, 0, 0, 0.65);
+  }
   .email-card img {
     width: 100%;
     height: auto;
@@ -245,6 +346,17 @@
     }
   }
 
+  /* ---- faq accordion --------------------------------------------------
+   * CSS grid-rows animation: 0fr collapsed, 1fr open. Smoother and more
+   * consistent cross-browser than animating <details>. */
+  .accordion-row {
+    display: grid;
+    grid-template-rows: 0fr;
+    transition: grid-template-rows 0.4s var(--ease);
+  }
+  .accordion-row.is-open { grid-template-rows: 1fr; }
+  .accordion-row > div { overflow: hidden; }
+
   /* ---- prose (blog body) --------------------------------------------- */
   .prose-inferno {
     font-size: 1.0625rem;
@@ -267,7 +379,7 @@
     scroll-margin-top: 6rem;
   }
   .prose-inferno a {
-    color: #ff5a5a;
+    color: #ff6a4d;
     text-decoration: underline;
     text-underline-offset: 3px;
     text-decoration-thickness: 1px;
@@ -337,6 +449,7 @@
   .marquee { animation: none; }
   .email-card img { transition: none; }
   .email-card:hover img, .email-card:focus-within img { transform: none; }
+  .card:hover, .email-card:hover, .btn:hover { transform: none; }
   *, *::before, *::after {
     animation-duration: 0.01ms !important;
     animation-iteration-count: 1 !important;
diff --git a/app/page.tsx b/app/page.tsx
index 20aa3da..6500e0b 100644
--- a/app/page.tsx
+++ b/app/page.tsx
@@ -1,7 +1,23 @@
 import Image from 'next/image';
 import Link from 'next/link';
 import type { Metadata } from 'next';
-import { ArrowRight, Calendar, Check, X } from '@phosphor-icons/react/dist/ssr';
+import {
+  ArrowRight,
+  Calendar,
+  Check,
+  Lightning,
+  ListChecks,
+  MagnifyingGlass,
+  PaintBrush,
+  Quotes,
+  Repeat,
+  Rocket,
+  ShieldCheck,
+  ChartLineUp,
+  TrendUp,
+  UsersThree,
+  X,
+} from '@phosphor-icons/react/dist/ssr';
 
 import Marquee from '@/components/Marquee';
 import EmailCard from '@/components/EmailCard';
@@ -36,11 +52,30 @@ export const metadata: Metadata = pageMeta({
    /work, /about, /contact and /free-email-audit is a section here, and the
    nav scrolls between them. /blog is the only route with a page of its own.
 
-   Eyebrow budget: ~12 sections, so at most 4 eyebrows and the hero is one of
-   them. Every other section opens on its headline alone. */
+   Eyebrow budget: 10 sections, so at most 4 badge pills. Used on the hero,
+   the proof section, the work section and the audit-checks section. Every
+   other section opens on its headline alone. */
 
 const ALL_FAQS = [...HOME_FAQS, ...AUDIT_FAQS];
 
+const SERVICE_ICON = {
+  'klaviyo-email-marketing': Lightning,
+  'email-design': PaintBrush,
+  'email-deliverability': ShieldCheck,
+  'retention-strategy': Repeat,
+} as const;
+
+const PROCESS_ICON = [MagnifyingGlass, Rocket, ChartLineUp];
+
+const AUDIT_ICON = [ListChecks, UsersThree, ShieldCheck, ChartLineUp];
+
+const HERO_STATS = [
+  { value: '44-48%', label: 'of revenue from email, across the client accounts below' },
+  { value: String(WORK.length), label: 'campaigns and flows designed from scratch, shown below' },
+  { value: SITE.founded, label: 'building ecommerce email programmes since' },
+  { value: '3', label: 'continents: Europe, Australia, United States' },
+];
+
 export default function HomePage() {
   const [lead, ...rest] = PROOF;
 
@@ -74,19 +109,24 @@ export default function HomePage() {
       />
 
       {/* ---------------------------------------------------------- HERO */}
-      <section className="pt-24 md:pt-24">
-        <div className="shell grid items-center gap-12 pb-16 lg:grid-cols-[1.15fr_0.85fr] lg:gap-14 lg:pb-24">
+      <section className="relative isolate pt-24 md:pt-24">
+        <div className="pointer-events-none absolute inset-0 overflow-hidden" aria-hidden>
+          <div className="orb orb-flame -top-40 -left-32 h-[26rem] w-[26rem] opacity-30" />
+          <div className="orb orb-ember top-10 -right-24 h-96 w-96 opacity-20" />
+        </div>
+
+        <div className="shell relative grid items-center gap-12 pb-16 lg:grid-cols-[1.15fr_0.85fr] lg:gap-14 lg:pb-24">
           <div className="reveal">
-            <p className="eyebrow">Email and retention marketing</p>
+            <p className="eyebrow">Email marketing for DTC &amp; ecommerce brands</p>
 
-            <h1 className="display-xl mt-6">
-              Your cheapest revenue is{' '}
-              <span className="accent">already on your list.</span>
+            <h1 className="display-xl mt-7">
+              Turn your email list into{' '}
+              <span className="accent">your most profitable channel.</span>
             </h1>
 
             <p className="lede mt-6">
-              Email marketing for ecommerce brands. We build the Klaviyo flows,
-              design the campaigns, and fix the deliverability underneath.
+              Klaviyo flows, campaigns designed in your brand, and the
+              deliverability work underneath so it actually reaches the inbox.
             </p>
 
             <div className="mt-9 flex flex-wrap gap-3">
@@ -110,7 +150,7 @@ export default function HomePage() {
           </div>
 
           {/* A real client email, not a div-built mock of one. */}
-          <div className="reveal" data-reveal-delay="120">
+          <div className="reveal relative" data-reveal-delay="120">
             <figure className="relative mx-auto max-w-md overflow-hidden rounded-xl border border-white/10 bg-ink-soft shadow-2xl shadow-black/60">
               <div className="h-[26rem] overflow-hidden md:h-[31rem]">
                 <Image
@@ -131,6 +171,25 @@ export default function HomePage() {
                 Girafon Bleu, welcome flow
               </figcaption>
             </figure>
+
+            <div className="chip absolute -bottom-6 left-6 text-xs sm:text-sm">
+              <TrendUp size={16} weight="bold" className="shrink-0 text-flame" aria-hidden />
+              <span>
+                <strong className="text-bone">47.96%</strong>{' '}
+                <span className="text-mute">of revenue from email</span>
+              </span>
+            </div>
+          </div>
+        </div>
+
+        <div className="shell relative">
+          <div className="reveal grid grid-cols-2 gap-x-6 gap-y-8 border-t border-white/8 pt-10 pb-10 sm:grid-cols-4 md:gap-x-10 md:pb-12">
+            {HERO_STATS.map((s) => (
+              <div key={s.label}>
+                <p className="accent font-display text-3xl md:text-4xl">{s.value}</p>
+                <p className="mt-2 text-[0.8125rem] leading-snug text-mute">{s.label}</p>
+              </div>
+            ))}
           </div>
         </div>
 
@@ -140,76 +199,49 @@ export default function HomePage() {
         </div>
       </section>
 
-      {/* ------------------------------------------------------ PROOF ---- *
-       * Asymmetric: one dashboard carries the section, two support it.    */}
+      {/* ------------------------------------------------------ PROOF ---- */}
       <section className="border-b border-white/8 bg-ink-raised">
         <div className="shell py-20 md:py-28">
           <div className="reveal max-w-3xl">
-            <p className="eyebrow">Receipts</p>
+            <p className="eyebrow">Receipts, not adjectives</p>
             <h2 className="display-lg mt-6">
-              Screenshots, <span className="accent">not adjectives.</span>
+              Screenshots of <span className="accent">real Klaviyo accounts.</span>
             </h2>
             <p className="lede mt-5">
-              Klaviyo dashboards from client accounts, exact reporting windows
-              shown. On these three, email accounted for 44% to 48% of total
-              store revenue.
+              Client dashboards, exact reporting windows shown. On these three,
+              email accounted for 44% to 48% of total store revenue.
             </p>
           </div>
 
-          <div className="mt-14 grid gap-6 lg:grid-cols-[1.35fr_1fr] lg:gap-8">
-            <figure className="reveal overflow-hidden rounded-xl border border-white/10 bg-ink">
-              <Image
-                src={lead.image}
-                alt={lead.alt}
-                width={lead.width}
-                height={lead.height}
-                sizes="(max-width: 1024px) 92vw, 52vw"
-                className="w-full border-b border-white/8 bg-white"
-              />
-              <figcaption className="p-7">
-                <p className="font-display text-3xl tracking-tight text-bone">
-                  {lead.headline}
-                </p>
-                <p className="mt-2 text-[1rem] text-flame">{lead.metric}</p>
-                <p className="mt-4 max-w-lg text-[0.9375rem] leading-relaxed text-mute">
-                  {lead.note}
-                </p>
-                <p className="mt-6 border-t border-white/8 pt-4 text-[0.75rem] uppercase tracking-[0.14em] text-mute/70">
-                  {lead.window}
-                </p>
-              </figcaption>
-            </figure>
-
-            <div className="grid gap-6 sm:grid-cols-2 lg:grid-cols-1">
-              {rest.map((p, i) => (
-                <figure
-                  key={p.image}
-                  className="reveal flex flex-col overflow-hidden rounded-xl border border-white/10 bg-ink"
-                  data-reveal-delay={(i + 1) * 110}
-                >
-                  <Image
-                    src={p.image}
-                    alt={p.alt}
-                    width={p.width}
-                    height={p.height}
-                    sizes="(max-width: 1024px) 92vw, 30vw"
-                    className="w-full border-b border-white/8 bg-white"
-                  />
-                  <figcaption className="flex flex-1 flex-col p-6">
-                    <p className="font-display text-xl tracking-tight text-bone">
-                      {p.headline}
-                    </p>
-                    <p className="mt-1.5 text-[0.875rem] text-flame">{p.metric}</p>
-                    <p className="mt-4 flex-1 text-[0.8125rem] leading-relaxed text-mute">
-                      {p.note}
-                    </p>
-                    <p className="mt-5 border-t border-white/8 pt-3.5 text-[0.6875rem] uppercase tracking-[0.14em] text-mute/70">
-                      {p.window}
-                    </p>
-                  </figcaption>
-                </figure>
-              ))}
-            </div>
+          <div className="mt-14 grid gap-6 md:grid-cols-3">
+            {[lead, ...rest].map((p, i) => (
+              <figure
+                key={p.image}
+                className="card reveal flex flex-col overflow-hidden rounded-xl"
+                data-reveal-delay={i * 100}
+              >
+                <Image
+                  src={p.image}
+                  alt={p.alt}
+                  width={p.width}
+                  height={p.height}
+                  sizes="(max-width: 768px) 92vw, 30vw"
+                  className="w-full border-b border-white/8 bg-white"
+                />
+                <figcaption className="flex flex-1 flex-col p-6 md:p-7">
+                  <p className="font-display text-2xl tracking-tight text-bone">
+                    {p.headline}
+                  </p>
+                  <p className="mt-1.5 text-[0.9375rem] text-flame">{p.metric}</p>
+                  <p className="mt-4 flex-1 text-[0.875rem] leading-relaxed text-mute">
+                    {p.note}
+                  </p>
+                  <p className="mt-5 border-t border-white/8 pt-3.5 text-[0.6875rem] uppercase tracking-[0.14em] text-mute/70">
+                    {p.window}
+                  </p>
+                </figcaption>
+              </figure>
+            ))}
           </div>
 
           <p className="reveal mt-9 max-w-2xl text-[0.8125rem] leading-relaxed text-mute/70">
@@ -221,8 +253,7 @@ export default function HomePage() {
       </section>
 
       {/* --------------------------------------------------- SERVICES ---- *
-       * Divided row list. The deliverables that used to sit behind four    *
-       * separate service pages are inline here, two columns per row.      */}
+       * Bento grid: what we do, made scannable in one screenful. */}
       <section id="services" className="scroll-mt-20 border-b border-white/8">
         <div className="shell py-20 md:py-28">
           <div className="reveal max-w-3xl">
@@ -231,41 +262,37 @@ export default function HomePage() {
             </h2>
             <p className="lede mt-5">
               Design without deliverability lands in spam. Flows without
-              strategy talk to the wrong people. You can buy these separately
-              from four suppliers and spend your week translating between them,
-              or have one team own the whole channel.
+              strategy talk to the wrong people. Buy these separately from four
+              suppliers and spend your week translating between them, or have
+              one team own the whole channel.
             </p>
           </div>
 
-          <ul className="mt-14 divide-y divide-white/9 border-t border-white/9">
-            {SERVICES.map((s, i) => (
-              <li
-                key={s.slug}
-                className="reveal grid gap-6 py-10 md:py-14 lg:grid-cols-[0.85fr_1.15fr] lg:gap-16"
-                data-reveal-delay={i * 70}
-              >
-                <div>
-                  <p className="font-display text-[0.875rem] tracking-[0.1em] text-flame">
-                    {s.index}
-                  </p>
-                  <h3 className="display-md mt-4">{s.title}</h3>
-                  <p className="mt-4 max-w-md text-[0.9375rem] leading-relaxed text-mute">
+          <div className="mt-14 grid gap-6 lg:grid-cols-2">
+            {SERVICES.map((s, i) => {
+              const Icon = SERVICE_ICON[s.slug as keyof typeof SERVICE_ICON];
+              return (
+                <div
+                  key={s.slug}
+                  className="card card-glow reveal flex flex-col rounded-xl p-8 md:p-9"
+                  data-reveal-delay={i * 70}
+                >
+                  <div className="flex h-11 w-11 shrink-0 items-center justify-center rounded-lg border border-flame/25 bg-gradient-to-br from-flame/20 to-ember/10 text-flame">
+                    <Icon size={20} weight="bold" aria-hidden />
+                  </div>
+                  <h3 className="display-md mt-6">{s.title}</h3>
+                  <p className="mt-3 text-[0.9375rem] leading-relaxed text-mute">
                     {s.summary}
                   </p>
-                </div>
 
-                <div>
-                  <p className="max-w-2xl text-[1rem] leading-relaxed text-bone/85">
-                    {s.intro}
-                  </p>
-                  <ul className="mt-7 grid gap-x-8 gap-y-3 sm:grid-cols-2">
+                  <ul className="mt-6 grid flex-1 gap-x-6 gap-y-3 sm:grid-cols-2">
                     {s.deliverables.map((d) => (
                       <li
                         key={d}
-                        className="flex gap-3 text-[0.875rem] leading-relaxed text-bone/75"
+                        className="flex gap-3 text-[0.8125rem] leading-relaxed text-bone/75"
                       >
                         <Check
-                          size={15}
+                          size={14}
                           weight="bold"
                           aria-hidden
                           className="mt-1 shrink-0 text-flame"
@@ -274,13 +301,14 @@ export default function HomePage() {
                       </li>
                     ))}
                   </ul>
-                  <p className="mt-7 border-l-2 border-flame pl-4 text-[0.9375rem] leading-relaxed text-mute">
+
+                  <p className="mt-6 border-t border-white/9 pt-5 text-[0.9375rem] leading-relaxed text-mute">
                     {s.outcome}
                   </p>
                 </div>
-              </li>
-            ))}
-          </ul>
+              );
+            })}
+          </div>
         </div>
       </section>
 
@@ -315,10 +343,10 @@ export default function HomePage() {
       </section>
 
       {/* ---------------------------------------------------- PROCESS ---- *
-       * Sticky split. Used once on this page.                             */}
+       * Three-step timeline with a connecting line. */}
       <section className="border-b border-white/8">
-        <div className="shell grid gap-12 py-20 md:py-28 lg:grid-cols-[0.85fr_1.15fr] lg:gap-16">
-          <div className="reveal lg:sticky lg:top-28 lg:self-start">
+        <div className="shell py-20 md:py-28">
+          <div className="reveal max-w-2xl">
             <h2 className="display-lg">
               Audit. Build. <span className="accent">Compound.</span>
             </h2>
@@ -328,20 +356,26 @@ export default function HomePage() {
             </p>
           </div>
 
-          <ol className="divide-y divide-white/9 border-t border-white/9">
-            {PROCESS.map((step, i) => (
-              <li
-                key={step.title}
-                className="reveal py-8 md:py-10"
-                data-reveal-delay={i * 90}
-              >
-                <h3 className="display-md">{step.title}</h3>
-                <p className="mt-4 max-w-xl text-[0.9375rem] leading-relaxed text-mute">
-                  {step.body}
-                </p>
-              </li>
-            ))}
-          </ol>
+          <div className="relative mt-16 grid gap-12 md:grid-cols-3 md:gap-10">
+            <div
+              aria-hidden
+              className="pointer-events-none absolute left-[16.6%] right-[16.6%] top-[1.375rem] hidden h-px bg-gradient-to-r from-flame/40 via-white/15 to-ember/40 md:block"
+            />
+            {PROCESS.map((step, i) => {
+              const Icon = PROCESS_ICON[i];
+              return (
+                <div key={step.title} className="reveal" data-reveal-delay={i * 100}>
+                  <div className="relative z-10 grid h-11 w-11 place-items-center rounded-full border border-flame/30 bg-ink text-flame">
+                    <Icon size={18} weight="bold" aria-hidden />
+                  </div>
+                  <h3 className="display-md mt-6">{step.title}</h3>
+                  <p className="mt-4 max-w-sm text-[0.9375rem] leading-relaxed text-mute">
+                    {step.body}
+                  </p>
+                </div>
+              );
+            })}
+          </div>
         </div>
       </section>
 
@@ -379,7 +413,7 @@ export default function HomePage() {
                   deliverability work underneath that decides whether any of it
                   arrives.
                 </p>
-                <p className="font-display text-[1.4rem] italic leading-[1.15] tracking-tight text-bone">
+                <p className="accent font-display text-[1.4rem] leading-[1.2] tracking-tight">
                   You grow, we grow. That is the entire business model.
                 </p>
               </div>
@@ -387,11 +421,11 @@ export default function HomePage() {
 
             <div className="reveal" data-reveal-delay="110">
               <h3 className="display-md">What we believe</h3>
-              <ol className="mt-7 border-t border-white/9">
+              <div className="mt-7 space-y-5">
                 {BELIEFS.map((b) => (
-                  <li key={b.index} className="border-b border-white/9 py-7">
-                    <div className="flex gap-5">
-                      <span className="font-display text-[0.875rem] tracking-[0.1em] text-flame">
+                  <div key={b.index} className="card rounded-xl p-6 md:p-7">
+                    <div className="flex gap-4">
+                      <span className="grid h-8 w-8 shrink-0 place-items-center rounded-full border border-flame/30 bg-flame/10 font-display text-[0.75rem] text-flame">
                         {b.index}
                       </span>
                       <div>
@@ -403,9 +437,9 @@ export default function HomePage() {
                         </p>
                       </div>
                     </div>
-                  </li>
+                  </div>
                 ))}
-              </ol>
+              </div>
             </div>
           </div>
 
@@ -417,7 +451,7 @@ export default function HomePage() {
             </h3>
 
             <div className="mt-14 grid gap-6 md:grid-cols-2">
-              <div className="reveal rounded-xl border border-white/10 bg-ink p-8 md:p-10">
+              <div className="card reveal rounded-xl p-8 md:p-10">
                 <h4 className="font-display text-lg tracking-tight text-flame">
                   Good fit
                 </h4>
@@ -440,7 +474,7 @@ export default function HomePage() {
               </div>
 
               <div
-                className="reveal rounded-xl border border-white/10 bg-ink p-8 md:p-10"
+                className="card reveal rounded-xl p-8 md:p-10"
                 data-reveal-delay="110"
               >
                 <h4 className="font-display text-lg tracking-tight text-mute">
@@ -482,37 +516,30 @@ export default function HomePage() {
         </div>
       </section>
 
-      {/* ----------------------------------------------- TESTIMONIALS ---- *
-       * Offset composition, no card chrome, same dark ground as the rest. */}
+      {/* ----------------------------------------------- TESTIMONIALS ---- */}
       <section className="border-b border-white/8">
         <div className="shell py-20 md:py-28">
           <h2 className="reveal display-lg max-w-3xl">
             Don’t just take <span className="accent">our word</span> for it.
           </h2>
 
-          <div className="mt-14 grid gap-x-12 gap-y-12 md:grid-cols-12">
-            {TESTIMONIALS.map((t, i) => {
-              const place = [
-                'md:col-span-5 md:col-start-1',
-                'md:col-span-5 md:col-start-8 md:mt-14',
-                'md:col-span-5 md:col-start-3',
-              ][i];
-              return (
-                <figure
-                  key={t.author}
-                  className={`reveal border-t border-white/12 pt-7 ${place}`}
-                  data-reveal-delay={i * 110}
-                >
-                  <blockquote className="font-display text-xl leading-snug tracking-tight text-bone md:text-2xl">
-                    “{t.quote}”
-                  </blockquote>
-                  <figcaption className="mt-6 text-[0.875rem]">
-                    <span className="text-bone">{t.author}</span>
-                    <span className="mt-0.5 block text-mute">{t.role}</span>
-                  </figcaption>
-                </figure>
-              );
-            })}
+          <div className="mt-14 grid gap-6 md:grid-cols-3">
+            {TESTIMONIALS.map((t, i) => (
+              <figure
+                key={t.author}
+                className="card reveal flex flex-col rounded-xl p-8"
+                data-reveal-delay={i * 100}
+              >
+                <Quotes size={28} weight="fill" aria-hidden className="text-flame/50" />
+                <blockquote className="mt-5 flex-1 font-display text-xl leading-snug tracking-tight text-bone">
+                  “{t.quote}”
+                </blockquote>
+                <figcaption className="mt-6 border-t border-white/9 pt-5 text-[0.875rem]">
+                  <span className="text-bone">{t.author}</span>
+                  <span className="mt-0.5 block text-mute">{t.role}</span>
+                </figcaption>
+              </figure>
+            ))}
           </div>
         </div>
       </section>
@@ -534,23 +561,26 @@ export default function HomePage() {
             </p>
           </div>
 
-          <ol className="mt-14 grid gap-6 md:grid-cols-2">
-            {AUDIT_CHECKS.map((c, i) => (
-              <li
-                key={c.index}
-                className="reveal rounded-xl border border-white/10 bg-ink p-8"
-                data-reveal-delay={(i % 2) * 90}
-              >
-                <span className="font-display text-[0.875rem] tracking-[0.1em] text-flame">
-                  {c.index}
-                </span>
-                <h3 className="display-md mt-4">{c.title}</h3>
-                <p className="mt-4 text-[0.9375rem] leading-relaxed text-mute">
-                  {c.body}
-                </p>
-              </li>
-            ))}
-          </ol>
+          <div className="mt-14 grid gap-6 md:grid-cols-2">
+            {AUDIT_CHECKS.map((c, i) => {
+              const Icon = AUDIT_ICON[i];
+              return (
+                <div
+                  key={c.index}
+                  className="card reveal rounded-xl p-8"
+                  data-reveal-delay={(i % 2) * 90}
+                >
+                  <div className="flex h-11 w-11 items-center justify-center rounded-lg border border-flame/25 bg-gradient-to-br from-flame/20 to-ember/10 text-flame">
+                    <Icon size={20} weight="bold" aria-hidden />
+                  </div>
+                  <h3 className="display-md mt-5">{c.title}</h3>
+                  <p className="mt-4 text-[0.9375rem] leading-relaxed text-mute">
+                    {c.body}
+                  </p>
+                </div>
+              );
+            })}
+          </div>
         </div>
       </section>
 
@@ -569,8 +599,11 @@ export default function HomePage() {
       {/* ---------------------------------------------------- CONTACT ---- *
        * Two ways in: book the call, or send the details and let us come    *
        * back to you. Same destination, different appetite for a calendar.  */}
-      <section id="contact" className="scroll-mt-20 bg-ink-raised">
-        <div className="shell grid gap-14 py-20 lg:grid-cols-[1fr_1.05fr] lg:gap-20 lg:py-28">
+      <section id="contact" className="scroll-mt-20 relative isolate overflow-hidden bg-ink-raised">
+        <div className="pointer-events-none absolute inset-0 overflow-hidden" aria-hidden>
+          <div className="orb orb-flame -bottom-40 -left-24 h-96 w-96 opacity-20" />
+        </div>
+        <div className="shell relative grid gap-14 py-20 lg:grid-cols-[1fr_1.05fr] lg:gap-20 lg:py-28">
           <div className="reveal">
             <h2 className="display-lg">
               Find out what your list is{' '}
@@ -640,7 +673,7 @@ export default function HomePage() {
           </div>
 
           <div
-            className="reveal rounded-xl border border-white/10 bg-ink p-7 md:p-10"
+            className="card reveal rounded-xl p-7 md:p-10"
             data-reveal-delay="110"
           >
             <h3 className="font-display text-xl tracking-tight">
diff --git a/components/AuditForm.tsx b/components/AuditForm.tsx
index 1343419..522f8a6 100644
--- a/components/AuditForm.tsx
+++ b/components/AuditForm.tsx
@@ -46,7 +46,7 @@ export default function AuditForm({ source = 'free-email-audit' }: { source?: st
     return (
       <div
         role="status"
-        className="rounded-lg border border-flame/40 bg-flame/6 p-8 md:p-10"
+        className="card rounded-xl border-flame/30 p-8 md:p-10"
       >
         <p className="eyebrow">Received</p>
         <h3 className="display-md mt-5">That’s all we need.</h3>
@@ -92,7 +92,7 @@ export default function AuditForm({ source = 'free-email-audit' }: { source?: st
               required={f.required}
               autoComplete={f.autoComplete}
               placeholder={'placeholder' in f ? f.placeholder : undefined}
-              className="mt-2.5 w-full rounded-lg border border-white/12 bg-ink px-4 py-3.5 text-[0.9375rem] text-bone outline-none transition-colors placeholder:text-mute/60 focus:border-flame"
+              className="field mt-2.5"
             />
           </div>
         ))}
@@ -110,7 +110,7 @@ export default function AuditForm({ source = 'free-email-audit' }: { source?: st
           name="message"
           rows={4}
           placeholder="e.g. flows are set up but barely earning, opens have been sliding for months, we send campaigns with no plan…"
-          className="mt-2.5 w-full resize-y rounded-lg border border-white/12 bg-ink px-4 py-3.5 text-[0.9375rem] text-bone outline-none transition-colors placeholder:text-mute/60 focus:border-flame"
+          className="field mt-2.5 resize-y"
         />
       </div>
 
diff --git a/components/CtaBand.tsx b/components/CtaBand.tsx
index 0e758d8..9581ee7 100644
--- a/components/CtaBand.tsx
+++ b/components/CtaBand.tsx
@@ -16,7 +16,9 @@ export default function CtaBand({
   body?: string;
 }) {
   return (
-    <section className="bg-ink-raised">
+    <section className="relative isolate overflow-hidden bg-ink-raised">
+      <div className="orb orb-flame -bottom-32 left-[8%] h-72 w-72 opacity-25" aria-hidden />
+      <div className="orb orb-ember -top-24 right-[10%] h-64 w-64 opacity-20" aria-hidden />
       <div className="shell py-20 md:py-28">
         <div className="reveal max-w-3xl">
           <h2 className="display-lg">
diff --git a/components/EmailCard.tsx b/components/EmailCard.tsx
index c491157..ad717ff 100644
--- a/components/EmailCard.tsx
+++ b/components/EmailCard.tsx
@@ -18,7 +18,7 @@ export default function EmailCard({
   priority?: boolean;
 }) {
   return (
-    <figure className="email-card group" tabIndex={0}>
+    <figure className="email-card group rounded-xl" tabIndex={0}>
       <div className="viewport">
         <Image
           src={item.image}
@@ -31,14 +31,14 @@ export default function EmailCard({
         />
       </div>
 
-      <figcaption className="flex items-baseline justify-between gap-4 border-t border-white/8 bg-ink-raised px-5 py-4">
+      <figcaption className="flex items-baseline justify-between gap-4 border-t border-white/8 bg-white/[0.03] px-5 py-4">
         <div>
           <p className="font-display text-base tracking-tight text-bone">
             {item.client}
           </p>
           <p className="mt-0.5 text-[0.8125rem] text-mute">{item.title}</p>
         </div>
-        <span className="shrink-0 text-[0.75rem] text-mute/70">
+        <span className="shrink-0 rounded-full border border-white/10 px-2.5 py-1 text-[0.6875rem] text-mute/80">
           {item.type}
         </span>
       </figcaption>
diff --git a/components/Faq.tsx b/components/Faq.tsx
index 7362292..f9cb161 100644
--- a/components/Faq.tsx
+++ b/components/Faq.tsx
@@ -1,35 +1,61 @@
-import { Plus } from '@phosphor-icons/react/dist/ssr';
+'use client';
+
+import { useState } from 'react';
+import { Plus } from '@phosphor-icons/react';
 
 /**
- * Native <details> accordion. Keyboard accessible, and the answers stay in
- * the HTML with no JS, which matters because they are the source for the
- * FAQ rich result.
+ * Accordion with a CSS grid-rows transition (0fr collapsed, 1fr open) so
+ * opening and closing animate smoothly in every browser, unlike a native
+ * <details> element. Multiple items can be open at once.
  */
 export default function Faq({
   items,
 }: {
   items: readonly { q: string; a: string }[];
 }) {
+  const [open, setOpen] = useState<Set<number>>(new Set());
+
+  const toggle = (i: number) =>
+    setOpen((prev) => {
+      const next = new Set(prev);
+      next.has(i) ? next.delete(i) : next.add(i);
+      return next;
+    });
+
   return (
     <div className="border-t border-white/9">
-      {items.map((item) => (
-        <details key={item.q} className="group border-b border-white/9">
-          <summary className="flex cursor-pointer list-none items-start justify-between gap-6 py-6 [&::-webkit-details-marker]:hidden">
-            <h3 className="font-display text-lg tracking-tight md:text-xl">
-              {item.q}
-            </h3>
-            <Plus
-              size={18}
-              weight="bold"
-              aria-hidden
-              className="mt-1 shrink-0 text-flame transition-transform duration-300 group-open:rotate-45"
-            />
-          </summary>
-          <p className="max-w-2xl pb-7 text-[0.9375rem] leading-relaxed text-mute">
-            {item.a}
-          </p>
-        </details>
-      ))}
+      {items.map((item, i) => {
+        const isOpen = open.has(i);
+        return (
+          <div key={item.q} className="border-b border-white/9">
+            <button
+              type="button"
+              onClick={() => toggle(i)}
+              aria-expanded={isOpen}
+              className="flex w-full cursor-pointer list-none items-start justify-between gap-6 py-6 text-left"
+            >
+              <h3 className="font-display text-lg tracking-tight md:text-xl">
+                {item.q}
+              </h3>
+              <Plus
+                size={18}
+                weight="bold"
+                aria-hidden
+                className={`mt-1 shrink-0 text-flame transition-transform duration-300 ${
+                  isOpen ? 'rotate-45' : ''
+                }`}
+              />
+            </button>
+            <div className={`accordion-row ${isOpen ? 'is-open' : ''}`}>
+              <div>
+                <p className="max-w-2xl pb-7 text-[0.9375rem] leading-relaxed text-mute">
+                  {item.a}
+                </p>
+              </div>
+            </div>
+          </div>
+        );
+      })}
     </div>
   );
 }
diff --git a/components/Nav.tsx b/components/Nav.tsx
index 7b0d5be..7cfd91e 100644
--- a/components/Nav.tsx
+++ b/components/Nav.tsx
@@ -88,9 +88,9 @@ export default function Nav() {
       <div ref={sentinel} aria-hidden className="absolute top-0 h-px w-full" />
 
       <header
-        className={`fixed inset-x-0 top-0 z-50 transition-colors duration-300 ${
+        className={`fixed inset-x-0 top-0 z-50 transition-all duration-300 ${
           scrolled || open
-            ? 'border-b border-white/8 bg-ink/92 backdrop-blur-md'
+            ? 'border-b border-white/10 bg-ink/80 shadow-[0_1px_0_rgba(255,255,255,0.04)] backdrop-blur-xl'
             : 'border-b border-transparent'
         }`}
       >
@@ -111,7 +111,7 @@ export default function Nav() {
                   >
                     {item.label}
                     {active && (
-                      <span className="absolute -bottom-1.5 left-0 h-px w-full bg-flame" />
+                      <span className="absolute -bottom-1.5 left-0 h-px w-full bg-flame shadow-[0_0_8px_rgba(245,23,23,0.85)]" />
                     )}
                   </Link>
                 </li>
